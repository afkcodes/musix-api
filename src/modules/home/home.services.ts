import { data } from "~/constants/data";
// import { BASE_URL, HOME_PATH } from "~/constants/http.constants";
// import { httpClient } from "~/helpers/httpClient";

export const homeService = async () => {
  // const url = `${BASE_URL}${HOME_PATH}`;
  const response = data // await httpClient(url);

  console

  return response
}